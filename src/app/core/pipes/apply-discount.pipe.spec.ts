import { ApplyDiscountPipe } from './apply-discount.pipe';

describe('ApplyDiscountPipe', () => {
  it('create an instance', () => {
    const pipe = new ApplyDiscountPipe();
    expect(pipe).toBeTruthy();
  });
});
